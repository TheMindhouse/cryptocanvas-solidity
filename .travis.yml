language: node_js
node_js:
  - '8.0.0'
install:
  - npm install -g truffle
  - truffle version
  - npm install -g ganache-cli
  - npm install
script:
- ganache-cli -p 7545 > /dev/null &
- sleep 5
- travis_wait 30 truffle test --compile-all --verbose-rpc